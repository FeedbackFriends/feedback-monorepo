## Scripts

Place setup and redeploy helpers here.

Current:
- `deploy-remote.sh`: used by `.github/workflows/deploy.yml`
- `local-db-reset.sh`: drops and recreates the local Postgres `feedback_db` database using `.env`
